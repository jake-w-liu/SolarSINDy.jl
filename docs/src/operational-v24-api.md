# V2.4e super-learner

The stage that serves the published center: the super-learner over the ten experts with its SINDy
family floor, the analog and direct-GBM experts it combines, the climatology relaxation, the
conformal depth strata that band it, and the depth-safe severity rule that decides the published
storm class from the served center and its logged predecessors.

The predecessor stages are documented in the [operational API](operational-api.md).

## V2.4e Super-Learner Serving

```@autodocs
Modules = [SolarSINDy]
Pages = ["operational_v24_serving.jl"]
Private = false
```


## Depth-Safe Severity

```@autodocs
Modules = [SolarSINDy]
Pages = ["serving_depth_safe.jl"]
Private = false
```
