docs/build/
docs/site/
storm_monitor.log
.DS_Store

data/omni_hourly_raw.dat
data/omni_extracted.csv
# joint ensemble draws: local-only, regenerated by validation/generate_ensemble_draws.jl
data/real_sindy_ensemble_draws.csv
# refit joint ensemble draws (11-term retrospective refit): local-only, same
# regeneration path as the frozen draws (see README "Refit prototype artifacts")
data/real_sindy_ensemble_draws_refit.csv
# generated figures (regenerable from validation scripts)
figs/

# bundled dashboard: vendored Plotly is large + regenerable (app/vendor-plotly.sh)
app/public/vendor/plotly.min.js
# dashboard server runtime log (local-only)
app/server.log

# Mutable operational monitor state and validation outputs.
var/
validation/output/

# local CLI configuration (may hold webhook URLs; template in deploy/solarsindy.env.example)
solarsindy.env
