# Historical Operational V2.0 Calibration

These point and conformal calibration files are paired with the historical
21-candidate, 10-active-term V2.0 core. They are retained for matched replay and
provenance only. Current issuance resolves calibration from the parent
`deploy/` directory and must identify the model as V2.1.
